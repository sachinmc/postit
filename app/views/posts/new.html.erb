<%= render 'shared/content_title', title: 'Create a new post' %>

<!-- by default, if a path is not specified, it will look for the partial
  by that name underneath the same directory
-->
<%= render 'form' %>
